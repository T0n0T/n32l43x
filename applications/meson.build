# 包含目录
applications_incdirs = include_directories(
  '.',
  # 'services',
  # 'services/cmd',
  # 'services/log',
  # 'tasks',
)

# 源文件
applications_sources = files(
  'main.c',
  'bootdfu.c',
  'bootloader.c',
  'bootsystim.c',
  'bootwdt.c',
)