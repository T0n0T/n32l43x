# 包含目录
applications_incdirs = include_directories(
  '.',
  'hello',
  'cmd',
  # 'services',
  # 'services/cmd',
  # 'services/log',
  # 'tasks',
)

# 源文件
applications_sources = files(
  'main.c',
  'bsp.c',
  'hello/hello.c',
  'cmd/cmd.c',
)